version: '3.8'
services:
  server:
    build: ./
    container_name: api_server
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: always                      